name: index
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  intl: ^0.19.0
  flutter_staggered_animations: ^1.1.1 
  http: ^1.2.1
  either_dart: ^1.0.0
  loading_animation_widget: ^1.2.0+4
  carousel_slider: ^4.2.1
  cached_network_image: ^3.3.1
  better_player: ^0.0.83 
  lottie: ^3.1.0
  shared_preferences: ^2.2.3
  cloud_firestore: ^4.17.0 
  flutter_color_models: ^1.4.0 # *** تأكد من تحديث هذا السطر ***

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/jsons/

